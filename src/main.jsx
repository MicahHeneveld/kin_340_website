import React from "react";
import { createRoot } from "react-dom/client";
import App from "../kin_340_study_website.jsx";
import "./styles.css";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
